# Team6Capstone — HMS Frontend (React + Vite)

This repository contains the **Hospital Management System (HMS)** frontend built using **React + Vite**.

## Project Setup

### 1) Install dependencies
```bash
npm install
